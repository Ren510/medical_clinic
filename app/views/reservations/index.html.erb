<%= flash[:notice] %>
    <div class="container">
      <div class="row">
        <div class="col-12 text-center">
            <br>
            <h1>予約画面</h1>
            <p>3ヶ月先まで予約することができます。</p>
            <%= link_to"予約確認画面",new_reservation_path,class: "nav-item nav-link" %>
        </div>
        <div class="col-12 mt-3">
            <%= week_calendar events: @reservations do |date, reservations| %>
            <%= date.day %>
            <% end %>
        </div>
      </div>
    </div>